# Pipfile
[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
openai = ">=1.0.0"
langchain = ">=0.1.0"
langchain-community = ">=0.0.11"
langchain-openai = ">=0.0.2"
fastapi = ">=0.100.0"
uvicorn = ">=0.24.0"
python-dotenv = ">=1.0.0"
pydantic = ">=2.0.0"
requests = ">=2.31.0"
slack-sdk = ">=3.26.0"
pymongo = ">=4.6.0"
bs4 = ">=0.0.2"
watchdog = ">=3.0.0"

[requires]
python_version = "3.13"